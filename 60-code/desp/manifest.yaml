artefact_type: code
visibility: restricted
audience:
  - NCB
  - PSP
form: text
role: implementation
status: normative
owner: ECB

# DISCLAIMER:
# The code in this folder is **illustrative and educational**.
# It does not represent official implementations, production-ready components,
# or endorsed technical approaches for the Digital Euro or any other real-world system.
#
# PURPOSE:
# This component serves as a reference implementation to demonstrate how technical
# specifications are refined into code while maintaining strict auditability,
# traceability, and mechanical governance across distinct institutional boundaries.
# This structure enables automated compliance verification and live impact analysis.

component:
  id: COMP-EUR-04
  name: Digital Euro Service Platform (DESP)
  description: >
    The central platform domain hosting the Settlement Engine,
    Alias Service, and core registries.
  role: platform-core
  ownership: eurosystem-development-team

implementation:
  technology_stack:
    language: java
    framework: spring-boot-3
    build_tool: maven

upstream:
  specifications:
    - id: SPEC-LIQ-FUNC
      version: 0.1
    - id: SPEC-SET-CORE
      version: 0.1
    - id: SPEC-ONB-FUNC
      version: 0.1

interfaces:
  exposed:
    - id: I_Settlement
      protocol: gRPC
      role: server
  consumed:
    - id: I_Alias_Registry
      protocol: gRPC
      role: client

assurance:
  tests:
    - set_id: OPS-LIQ
      coverage: partial